// Resolvers no longer needed - settings are loaded via config API
// This file is kept for potential future use

export function* getCourseSettings(postId) {
    // Settings are now loaded directly in Settings.js via config API
    // No async resolution needed
    return;
}
