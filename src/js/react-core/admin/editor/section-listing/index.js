import { renderBlock } from '../../../utility/renderBlock';
import SectionListingHeader from './SectionListingHeader';

// Render the section listing header.
renderBlock('splms-section-listing-header-wrapper', <SectionListingHeader />);
